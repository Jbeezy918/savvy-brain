# Tools Registry

Place standalone utilities here, including form-filling logic, GovCon parsers, and media-generation pipelines. Each utility should expose a clear command or Python interface and document its dependencies.

