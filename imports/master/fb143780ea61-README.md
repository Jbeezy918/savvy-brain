# Savvy Brain

Local workspace for project knowledge, automation agents, reusable tools, archives, and the centralized Streamlit dashboard.

## Dashboard

Install dependencies with `python -m pip install -r requirements.txt`, then run `streamlit run dashboard/app.py` from this directory.

